import React from 'react';
import Button from './Button/Button';

function App() {
  return (
    <div className="App">
      {/* <Button text="BTN" ratio="square" onClick={() => alert('it works!!')} />
      <Button text="Regular button" />
      <Button text="BIG button" size="big" />
      <Button text="Big Alert Button" size="big" color="alert" /> */}
      <Button />
    </div>
  );
}

export default App;
